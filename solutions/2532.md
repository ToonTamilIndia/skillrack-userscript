# Id 2532 - Triangle - Smallest Side

```c
#include <stdio.h>
int main()
{
    int X,Y,Z;
    scanf("%d%d%d",&X,&Y,&Z);
    int smallest=X;
    if(Y < smallest)
    {
        smallest = Y;
    }

    if(Z < smallest)
    {
        smallest=Z;
    }

    printf("%d",smallest);
}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include <stdio.h> int main() { int X,Y,Z; scanf("%d%d%d",&X,&Y,&Z); int smallest=X; if(Y < smallest) { smallest = Y; } if(Z < smallest) { smallest=Z; } printf("%d",smallest); }
