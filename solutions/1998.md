# Id 1998 - Positive Negative or Zero

```c
#include<stdio.h>

int main()
{
    int number;
    scanf("%d",&number);
    if(number == 0){
        printf("zero");
    }
    else
    if(number > 0){
        printf("positive");
    }else{
        printf("negative");
    }

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> int main() { int number; scanf("%d",&number); if(number == 0){ printf("zero"); } else if(number > 0){ printf("positive"); }else{ printf("negative"); } }
