# Id 2192 - Senior or Not

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    int age;
    scanf("%d",&age);
    if(age >= 60){
        printf("senior");
    }else{
        printf("notsenior");
    }

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { int age; scanf("%d",&age); if(age >= 60){ printf("senior"); }else{ printf("notsenior"); } }
