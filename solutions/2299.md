# Id 2299 - Interlace odd / even from A to B.

```c
#include<stdio.h>
int main()
{
    int X, Y, i, j;
    scanf("%d%d", &X, &Y);
    for( i = X, j = Y; i <= Y; i++, j--)
    {
        if( i % 2 == 1)
        {
            printf("%d ", i);
        }
        if( j % 2 == 0)
        {
            printf("%d ", j);
        }
    }
}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> int main() { int X, Y, i, j; scanf("%d%d", &X, &Y); for( i = X, j = Y; i <= Y; i++, j--) { if( i % 2 == 1) { printf("%d ", i); } if( j % 2 == 0) { printf("%d ", j); } } }
