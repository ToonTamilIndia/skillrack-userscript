# Id 9678 - First K Largest Integers

```c
#include <stdio.h>
#include <stdlib.h>

void heapify(int *arr, int n, int i) {
    int smallest = i;
    int left = 2 * i + 1;
    int right = 2 * i + 2;

    if (left < n && arr[left] < arr[smallest])
        smallest = left;
    if (right < n && arr[right] < arr[smallest])
        smallest = right;

    if (smallest != i) {
        int temp = arr[i];
        arr[i] = arr[smallest];
        arr[smallest] = temp;
        heapify(arr, n, smallest);
    }
}

int main() {
    int n;
    scanf("%d", &n);

    int *arr = (int *)malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int k;
    scanf("%d", &k);

    int *heap = (int *)malloc(k * sizeof(int));
    for (int i = 0; i < k; i++) {
        heap[i] = arr[i];
    }

    for (int i = k / 2 - 1; i >= 0; i--) {
        heapify(heap, k, i);
    }

    for (int i = k; i < n; i++) {
        if (arr[i] > heap[0]) {
            heap[0] = arr[i];
            heapify(heap, k, 0);
        }
    }

    int *result = (int *)malloc(k * sizeof(int));
    for (int i = 0; i < k; i++) {
        result[i] = heap[0];
        heap[0] = heap[k - 1 - i];
        heapify(heap, k - 1 - i, 0);
    }

    for (int i = k - 1; i >= 0; i--) {
        if (i == k - 1)
            printf("%d", result[i]);
        else
            printf(" %d", result[i]);
    }
    printf("\n");

    free(arr);
    free(heap);
    free(result);

    return 0;
}
```

Verified: passed the SkillRack judge on 2026-09-10 via auto solver
