# Id 2204 - Print Social Network

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    char L;
    L = getchar();
    L = toupper(L);
    switch(L){
     case 'W':
        printf("whatsapp");
        break;
     case 'T':
        printf("twitter");
        break;
     case 'F':
        printf("facebook");
        break;
     default:
        printf("undefined");
    }

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { char L; L = getchar(); L = toupper(L); switch(L){ case 'W': printf("whatsapp"); break; case 'T': printf("twitter"); break; case 'F': printf("facebook
