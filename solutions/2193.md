# Id 2193 - Largest among three numbers

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    int A,B,C;
    scanf("%d%d%d",&A,&B,&C);
    int largest=A;
    if(B > largest){
        largest = B;
    }
    
    if(C > largest){
        largest = C;
    }
    
    printf("%d",largest);

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { int A,B,C; scanf("%d%d%d",&A,&B,&C); int largest=A; if(B > largest){ largest = B; } if(C > largest){ largest = C; } printf("%d",largest); }
