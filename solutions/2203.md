# Id 2203 - Square or Rectangle

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    int length,breadth;
    scanf("%d%d",&length,&breadth);

    if(length == breadth){
        printf("SQUARE");
    }else
    {
        printf("RECTANGLE");
    }

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { int length,breadth; scanf("%d%d",&length,&breadth); if(length == breadth){ printf("SQUARE"); }else { printf("RECTANGLE"); } }
