# Id 1999 - Print smaller number

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    int A,B;
    scanf("%d%d",&A,&B);
    if(A == B)
    {
        printf("EQUAL");
    }
    else
    {
        printf("%d", A < B ? A:B);
    }
}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { int A,B; scanf("%d%d",&A,&B); if(A == B) { printf("EQUAL"); } else { printf("%d", A < B ? A:B); } }
