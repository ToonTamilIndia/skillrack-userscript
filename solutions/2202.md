# Id 2202 - Two Numbers Sum - Even or Odd

```c
#include<stdio.h>
#include <stdlib.h>

int main()
{
    int M,N;
    scanf("%d%d",&M,&N);
    int sum = M+N;
    if(sum%2 == 0){
        printf("even");
    }else
    {
        printf("odd");
    }

}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> #include <stdlib.h> int main() { int M,N; scanf("%d%d",&M,&N); int sum = M+N; if(sum%2 == 0){ printf("even"); }else { printf("odd"); } }
