# Id 2301 - Multiple of N till divisible by X.

```c
#include<stdio.h>

int main()
{
    int N,X;
    scanf("%d%d",&N,&X);

    int multiple=N;
    int run=1;
    while(run)
    {
        printf("%d ",multiple);

        if(multiple%X == 0)
        {
            run=0;
        }

        multiple+=N;
    }
}
```

Verified: passed the SkillRack judge sample test case(s) on 2026-09-05 via the userscript auto solver (DuckDuckGo claude-haiku-4-5). Sample: #include<stdio.h> int main() { int N,X; scanf("%d%d",&N,&X); int multiple=N; int run=1; while(run) { printf("%d ",multiple); if(multiple%X == 0) { run=0; } multiple+=N; } }
