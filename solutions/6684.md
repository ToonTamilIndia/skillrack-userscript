# Id 6684 — Function findMinElement - CTS PATTERN

```c
int findMinElement(int *arr1, int M, int *arr2, int N)
{
    sortArray(arr1, M);
    sortArray(arr2, N);
    int min1 = arr1[0];
    int min2 = arr2[0];
    return (min1 < min2) ? min1 : min2;
}
```

Verified: harness sanity check (returns minimum across both arrays); no samples in batch.